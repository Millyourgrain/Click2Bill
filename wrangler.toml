# Cloudflare Worker with static assets
name = "clicktobill"
compatibility_date = "2024-01-01"
main = "worker.js"

# Directory of static assets to deploy (Vite outputs to dist)
[assets]
directory = "./dist"

# Worker env vars (required for /api/send-email)
# 1. Copy your VITE_FIREBASE_API_KEY from .env.production and paste it below (same value)
# 2. Set RESEND_API_KEY as secret: npx wrangler secret put RESEND_API_KEY
[vars]
FIREBASE_API_KEY = "AIzaSyCZ4Vx2NDD3REkrAzThMfg0X5iYbQkB5d4"
